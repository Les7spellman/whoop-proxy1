{
  "name": "whoop-proxy1",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth + WHOOP v2 read proxy for the Athlete Management System (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
